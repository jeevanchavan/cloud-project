apiVersion: v1
kind: Service

metadata:
  name: redis-service
  namespace: vibetrack
spec:
  selector:
    app: redis

  ports:
    - protocol: TCP
      port: 19121
      targetPort: 19121

  type: ClusterIP